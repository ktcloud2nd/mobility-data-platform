# 사용자 파드 (2개)
# 실제 사용자 웹 이미지로 수정 필요

apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-web
  namespace: user-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-web
  template:
    metadata:
      labels:
        app: user-web
    spec:
      containers:
        - name: user-web
          image: nginx:1.27
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
